VITE_API_URL=http://localhost:5000
# Optional: API Gateway WebSocket URL for real-time notifications (e.g. wss://xxx.execute-api.region.amazonaws.com/dev)
# When unset, frontend falls back to polling /notifications/unread every 30s
# VITE_WEBSOCKET_URL=
# Set to false for local dev with backend Testing:UseLocalJwt (e.g. Docker)
VITE_USE_COGNITO=true
